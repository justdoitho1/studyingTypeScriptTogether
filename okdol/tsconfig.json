{
    "compilerOptions" :{
        "noEmitOnError": true, //모든 에러가 고쳐진 뒤에 js파일 생성
        "target": "es6", // default
        "watch": true, // watch for changes 
        "outDir": "./dist", // output directory. 생성된 자바스크립트 파일을 이곳에 저장한다.
        "lib" : ["dom","ES6"], // 이 라이브러리 추가해야 console.log()같은 웹 api 인식
        

    }
}